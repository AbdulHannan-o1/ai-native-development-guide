{
  "version": 2,
  "installCommand": "ls -R && cd api && pip install -r requirements.txt && cd .. && pnpm install",
  "buildCommand": "pnpm build",
  "functions": {
    "book-source/api/src/main.py": {
      "runtime": "python3.10",
      "memory": 1024,
      "maxDuration": 10
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "book-source/api/src/main.py"
    },
    {
      "src": "/(.*)",
      "dest": "build/$1"
    }
  ],
  "outputDirectory": "build"
}