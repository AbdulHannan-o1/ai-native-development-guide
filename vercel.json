{
  "version": 2,
  "installCommand": "pip install -r api/requirements.txt && pnpm install",
  "buildCommand": "pnpm build",
  "builds": [
    {
      "src": "api/src/main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "build/$1"
    }
  ],
  "outputDirectory": "build"
}