{
  "version": 2,
  "installCommand": "ls -R && cd book-source/api && pip install -r requirements.txt && cd ../.. && pnpm install --prefix book-source",
  "buildCommand": "pnpm build --prefix book-source",
  "functions": {
    "book-source/api/src/main.py": {
      "runtime": "python3.10",
      "memory": 1024,
      "maxDuration": 10
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "book-source/api/src/main.py"
    },
    {
      "src": "/(.*)",
      "dest": "book-source/build/$1"
    }
  ],
  "outputDirectory": "book-source/build"
}